# =====================================================
# SHOOTING RANGE DASHBOARD - ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your values
# =====================================================

# Node Environment
NODE_ENV=production

# Database Configuration (Aiven MySQL)
DB_HOST=your-aiven-mysql-host.aivencloud.com
DB_PORT=12345
DB_USER=avnadmin
DB_PASSWORD=your-secure-password
DB_NAME=shooting_range_db

# SSL Certificate (Base64 encoded)
# To convert your ca.pem to base64:
# On Windows PowerShell: [Convert]::ToBase64String([IO.File]::ReadAllBytes("ca.pem"))
# On Linux/Mac: base64 -w 0 ca.pem
DB_SSL_CA_BASE64=your-base64-encoded-certificate

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-vercel-app.vercel.app,https://your-custom-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Server Configuration
PORT=3001

